%h1 ユーザーがわのbooks

%table
  %thead
    %tr
      %th Title
      %th Price
      %th Released on
      %th
      %th
      %th

  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.price
        %td= book.released_on
        %td= link_to '詳細', book_path(book)
