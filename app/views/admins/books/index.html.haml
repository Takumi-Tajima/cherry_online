%h1 Listing books

%table
  %thead
    %tr
      %th Title
      %th Price
      %th Released on
      %th
      %th
      %th

  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.price
        %td= book.released_on
        %td= link_to 'Show', admins_book_path(book)
        %td= link_to 'Edit', edit_admins_book_path(book)
        %td= button_to '削除', admins_book_path(book), method: :delete

%br

= link_to 'New Book', new_admins_book_path
